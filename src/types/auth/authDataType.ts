export interface LoginDataType {
  email: string;
  password: string;
}

export interface JoinDataType {
  email: string;
  password: string;
  name: string;
  phone: string;
}

export interface VaildType {
  status: boolean;
  text: string;
}
